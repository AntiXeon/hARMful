#ifndef _DOOMFUL_IMODEL_
#define _DOOMFUL_IMODEL_


namespace MVC {

    /**
     * @brief   An IModel handles only its own data, with a lot of operations on
     *          it as inner calculations. Such data cannot be modified directly
     *          by an external object.
     */
    class IModel {
    } ;

} ;


#endif